@model ApplicationUser

@{
    ViewData["Title"] = "Supprimer l'utilisateur";
}

<h1>Supprimer l'utilisateur</h1>

<h3>Êtes-vous sûr de vouloir supprimer cet utilisateur ?</h3>
<div class="alert alert-danger">
    <strong>Attention !</strong> Cette action est irréversible. L'utilisateur et toutes ses données associées seront supprimés.
</div>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-3">Email</dt>
        <dd class="col-sm-9">@Model.Email</dd>

        <dt class="col-sm-3">Prénom</dt>
        <dd class="col-sm-9">@Model.Prenom</dd>

        <dt class="col-sm-3">Nom</dt>
        <dd class="col-sm-9">@Model.Nom</dd>

        <dt class="col-sm-3">Date de création</dt>
        <dd class="col-sm-9">@Model.DateCreation.ToShortDateString()</dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Supprimer" class="btn btn-danger" />
        <a asp-action="Index" class="btn btn-secondary">Annuler</a>
    </form>
</div>